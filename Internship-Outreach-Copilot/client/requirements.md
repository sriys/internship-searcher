## Packages
framer-motion | Smooth transitions and animations for a premium feel
lucide-react | Beautiful icons for the interface
date-fns | Formatting dates for display

## Notes
- Tailwind config needs to extend font family for 'Inter' and 'Outfit' (display font).
- API routes are defined in @shared/routes.
- Uses Shadcn UI components which are already present in the project structure.
